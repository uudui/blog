= form_for(@post) do |f|
  - if @post.errors.any?
    .alert.alert-block.alert-error
      p
        strong
          = t('activerecord.errors.prohibited_being_saved')
      ul
        - @post.errors.full_messages.each do |msg|
          li= msg
  .control-group
    = f.label :content, :class => "control-label" do
      abbr title="required"
      = '* '
      = t 'posts.content'
    .controls
      = f.text_area :content, :class => "input-block-level", :rows => 10, :required => true, :placeholder => t('posts.content_placeholder')
  .control-group
    = f.label :tip, t('posts.tip'), :class => "control-label"
    .controls
      = f.text_area :tip, :class => "input-block-level", :rows => 2,  :placeholder => t('posts.tip_placeholder')
  .control-group
    .controls
      = f.submit t('posts.submit'), :class => "btn btn-large btn-success"